import { spacing } from './spacing'

export const layout = {
    screenPadding: spacing.lg,
    sectionGap: spacing['2xl'],
    cardPadding: spacing.lg,
    cardGap: spacing.md,
    listGap: spacing.sm,
} as const
